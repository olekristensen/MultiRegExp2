{
  "name": "multi-regexp2",
  "version": "0.0.1",
  "main": "multiRegExp2-compiled.js",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.22.0"
  },
  "scripts": {
    "build": "babel src --out-dir lib --source-maps --presets es2015",
    "test": "node lib/multiRegExp2-test.js",
    "best": "npm run build && npm run test",
    "prepare":  "npm run build"
  },
  "dependencies": {}
}
